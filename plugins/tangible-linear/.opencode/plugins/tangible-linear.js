// opencode plugin entry.
// The Tangible Linear conventions are delivered as instructions (AGENTS.md) and as a
// skill (skills/tangible-linear, symlinked into ~/.agents/skills by bin/install.mjs).
// This is a no-op plugin so opencode loads the package cleanly.
export const tangibleLinear = async () => ({});
