---
description: Handle a Linear request using Tangible's conventions (tangible-linear skill)
argument-hint: [what to create / update in Linear]
---
Invoke the `tangible-linear` skill and follow it to handle the following Linear
request for the **Tangible Careers** workspace. Use the Linear MCP tools to act,
and apply the team conventions (sprint date-labels, the label taxonomy, status
hygiene, blocked-by + `blocked`, the project "does it end?" test).

Request:

$ARGUMENTS
