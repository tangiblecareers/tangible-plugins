---
description: Gather an engineer's current work into 3 buckets (In progress / To do / Backlog) for sprint planning
argument-hint: [whose work / which area, optional]
---
Invoke the `tangible-linear` skill and run its "gather someone's work" workflow for
the **Tangible Careers** team.

Produce three buckets — **In progress**, **To do (committed)**, **Backlog (someday)** —
with a title + one line of context per item, ready to send to Aaryash before sprint
planning. Pull from Linear and the current repo/context as needed. Keep it plain; these
become proper issues together later.

Scope / whose work: $ARGUMENTS
